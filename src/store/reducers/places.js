const initialState = {
  placeName : '',
  places : [],
  placeSelected : null
};

const reducer = (state = initialState, action) => {
  switch(action.type) {
    default :
      return state;
  }
};

export default reducer;
